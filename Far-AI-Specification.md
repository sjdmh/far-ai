# 📘 Far AI — سند مشخصات نسخه ۱ (Specification)

> **سند مرجع محصول** برای ساخت «کارمند دیجیتال» آژانس فَر
> پروژه داخلی: **Far OS** — نسخه: **v0.2** — تاریخ: ۲ مرداد ۱۴۰۵
> وضعیت: **کد نسخه ۱ کامل و تست‌شده است ✅ — منتظر استقرار روی سرور**

## ✅ وضعیت پیشرفت (آخرین به‌روزرسانی)

| بخش | وضعیت |
|---|---|
| سند مشخصات | ✅ کامل |
| Backend (FastAPI + OpenAI + PostgreSQL) | ✅ نوشته و تست‌شده |
| تلگرام بات | ✅ نوشته‌شده (آماده نصب روی سرور) |
| پلاگین وردپرس (ویجت چت سایت) | ✅ ZIP آماده نصب |
| تشخیص سرویس (Intent) | ✅ نوشته و تست‌شده |
| نیازسنجی + استخراج لید | ✅ تست‌شده با ۲۰ سناریو |
| امتیازدهی لید (Lead Scoring) | ✅ تست‌شده |
| اعلان تلگرام تیم + ایمیل | ✅ نوشته‌شده (فعال با تنظیم .env) |
| دانش‌نامه فَر + RAG | ✅ نوشته و تست‌شده |
| داشبورد ساده | ✅ نوشته‌شده (`/dashboard`) |
| Docker + Nginx + بکاپ | ✅ نوشته‌شده |
| استقرار روی سرور واقعی | ⏳ قدم بعدی |


---

## ۱) چکیده

Far AI یک دستیار هوشمند است که در **سایت فَر** و **تلگرام فَر** به مشتریان پاسخ می‌دهد، نیاز آن‌ها را شناسایی می‌کند، لید می‌سازد، امتیازدهی می‌کند و به تیم اطلاع می‌دهد.

**هدف یک‌خطی:**
> تا ۳۰ روز دیگر، هر مشتری که وارد سایت یا تلگرام فَر شود، یک پاسخ حرفه‌ای با لحن برند فَر بگیرد و به یک لید قابل پیگیری تبدیل شود.

---

## ۲) تعریف موفقیت نسخه ۱ (قابل اندازه‌گیری)

| معیار | هدف |
|---|---|
| زمان پاسخ‌گویی | کمتر از ۵ ثانیه |
| رسیدن مکالمه به نیازسنجی کامل (۳+ سؤال) | حداقل ۷۰٪ مکالمات |
| ارسال لید داغ (امتیاز > ۶۰) به تیم | حداکثر ۱ دقیقه بعد از پایان مکالمه |
| ثبت تمام مکالمات در دیتابیس | ۱۰۰٪ |

---

## ۳) شخصیت Far AI

### هویت
- **نام:** Far AI — دستیار هوشمند آژانس تبلیغاتی فَر
- **نماد برند:** طاووس 🦚 — مظهر شکوه، تناسب و تکامل
- **شعار آژانس:** «فر؛ شکوه تکامل برند شما»
- **نقش:** اولین نقطه تماس آژانس — بریف‌گیر دیجیتال (کشف نیاز مثل یک کارشناس)
- **ویژگی‌ها:** حرفه‌ای، خلاق، صمیمی، باوقار
- **زبان:** فارسی روان (بدون غلط، بدون لحن خشک)
- **تجربه آژانس:** بیش از ۱۰ سال — تهران — تلفن: ۰۲۱۹۱۰۹۲۰۷۳

### System Prompt (بر اساس برند واقعی فَر)

```
تو Far AI هستی؛ دستیار هوشمند آژانس تبلیغاتی «فَر».
شعار آژانس: «فر؛ شکوه تکامل برند شما» — نماد: طاووس 🦚 — بیش از ۱۰ سال تجربه.
خدمات: [طراحی لوگو، هویت بصری، عکاسی تبلیغاتی، تیزر و ویدئوگرافی، استاپ موشن، دوره آموزش گرافیک]

قوانین طلایی تو:
1. لحن تو حرفه‌ای، خلاق، صمیمی و باوقار است؛ هرگز خشک یا رباتیک جواب نده.
2. هدف تو فروش اجباری نیست؛ هدف تو «کشف نیاز» مشتری است — مثل یک بریف‌گیر حرفه‌ای.
3. در پایان هر پاسخ، یک سؤال باز بپرس تا گفتگو ادامه پیدا کند.
4. درباره قیمت هرگز قیمت قطعی نده؛ بگو بعد از جلسه هماهنگی و دریافت بریف اعلام می‌شود
   و اول حوزه کاری، نوع پروژه و بودجه حدودی را بپرس.
5. اگر سؤالی خارج از خدمات فَر پرسیده شد، مودبانه پاسخ بده و گفتگو را به خدمت مناسب برگردان.
6. اگر مشتری عصبانی یا سردرگم شد، همدلی کن و پیشنهاد اتصال به همکار انسانی بده
   (تلفن: ۰۲۱۹۱۰۹۲۰۷۳ یا ۰۹۱۴۷۲۱۷۶۹۰).
7. اطلاعات تماس را در اواخر مکالمه و بعد از کشف نیاز بگیر، نه در ابتدا.
8. فارسی روان و بدون غلط بنویس؛ اصطلاحات تخصصی را ساده توضیح بده.
9. وقتی نیاز مشخص شد، مشتری را به «مشاوره رایگان» و «فرم بریف» دعوت کن.
```

---

## ۴) خدمات فَر (مبنای تشخیص نیاز) — بر اساس سایت واقعی farr-agency.com

| خدمت | کلمات کلیدی تشخیص | توضیح کوتاه برای مشتری |
|---|---|---|
| طراحی لوگو | لوگو، آرم، لوگوتایپ، نشان، لوگو موشن، لوگوی تجاری/صنعتی/جواهری/دندانپزشکی/کلینیک/آرایشگاه/مبلمان | لوگوی اختصاصی و مفهومی بر اساس کانسپت و اهداف برند — انواع: لوگوتایپ، لوگو موشن، تجاری، صنعتی، جواهری و... |
| هویت بصری | هویت بصری، برندبوک، پالت رنگی، فونت اختصاصی، ست اداری | برندبوک، پالت رنگی، فونت و المان‌های بصری برای یکپارچگی برند |
| عکاسی تبلیغاتی | عکاسی، عکس محصول، فود، عکاسی صنعتی | عکاسی حرفه‌ای محصول، فود و صنعتی |
| تیزر و ویدئوگرافی | تیزر، ویدیو/ویدئو، موشن گرافیک، رپورتاژ، فیلم تبلیغاتی | تیزر معرفی محصول، رپورتاژ ویدیویی، فیلم تبلیغاتی برند |
| استاپ موشن | استاپ موشن، استاپ‌موشن | انیمیشن استاپ‌موشن خلاقانه برای معرفی برند |
| دوره آموزش گرافیک | دوره، آموزش گرافیک، فرپدیا، یادگیری | دوره جامع آموزش گرافیک دیزاین — «ماهیگیری یاد می‌دیم، نه ماهی» |

**نکته:** دانش کامل‌تر در پوشه `backend/knowledge/` است (خدمات، فرآیند، FAQ، نمونه‌کارها، قیمت‌گذاری، داستان برند).

---

## ۵) جریان مکالمه (Conversation Flow)

### فازهای گفتگو

```
مشتری وارد می‌شود (سایت / تلگرام)
        │
        ▼
┌─────────────────────────────────────────────┐
│  فاز ۰: خوش‌آمد                        │
│  «سلام 👋 به آژانس فَر خوش اومدی!        │
│   من Far AI هستم، دستیار هوشمند فَر.    │
│   بگو ببینم دنبال چی هستی؟»            │
└─────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────┐
│  فاز ۱: کشف نیاز                        │
│  • تشخیص سرویس از پیام (جدول بخش ۴)      │
│  • سؤال تأییدی: «پس برای [خدمت] نیاز   │
│    داری، درسته؟»                        │
└─────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────┐
│  فاز ۲: نیازسنجی عمیق                   │
│  • ۵ سؤال کلیدی (زیر)                    │
│  • ثبت پاسخ‌ها برای امتیازدهی            │
└─────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────┐
│  فاز ۳: جمع‌آوری اطلاعات تماس            │
│  نام ← تلگرام/شماره ← شرکت ← نقش تصمیم‌گیرنده │
└─────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────┐
│  فاز ۴: بستن مکالمه                     │
│  خلاصه نیاز + وعده تماس + تشکر           │
│  ← ذخیره لید ← امتیازدهی ← اعلان به تیم  │
└─────────────────────────────────────────────┘
```

### ۵ سؤال کلیدی نیازسنجی (سوالات فاز ۲) — به سبک بریف‌گیری آژانس

1. حوزه کاری شما چیه؟ چه کسب‌وکاری دارید؟
2. دقیقاً به کدوم خدمت فَر نیاز دارید؟ (لوگو / هویت بصری / عکاسی تبلیغاتی / تیزر و ویدئوگرافی / استاپ موشن / دوره آموزش گرافیک)
3. (اگر لوگو/هویت بصری) آیا قبلاً لوگو دارید یا از صفر می‌خواید؟ / هدف اصلی پروژه چیه؟ (معرفی برند / جذب مشتری / شروع کسب‌وکار)
4. چه زمانی می‌خواید شروع بشه؟ فوریت چقدره؟
5. بودجه حدودی برای این کار چقدره؟

### تشخیص سرویس (مثال)

| پیام مشتری | سرویس تشخیص‌داده‌شده |
|---|---|
| «می‌خوایم برندمون لوگو داشته باشه» | طراحی لوگو |
| «هویت بصری و برندبوک می‌خوایم» | هویت بصری |
| «برای محصولمون عکاسی تبلیغاتی لازم داریم» | عکاسی تبلیغاتی |
| «تیزر معرفی محصول می‌خوایم» | تیزر و ویدئوگرافی |
| «استاپ موشن برای برندمون می‌خوایم» | استاپ موشن |
| «می‌خوام گرافیک یاد بگیرم» | دوره آموزش گرافیک |

---

## ۶) مدل داده — جدول customers (نسخه اول)

| ستون | نوع | توضیح |
|---|---|---|
| `id` | serial | کلید اصلی |
| `name` | text | نام مشتری |
| `phone` | text | شماره تماس (اختیاری) |
| `telegram_id` | text | آیدی تلگرام / چت‌آیدی |
| `company` | text | نام کسب‌وکار |
| `service` | text[] | خدمات مورد نیاز (تشخیص‌داده‌شده) |
| `lead_score` | integer | امتیاز لید (۰ تا ۱۰۰) |
| `conversation` | jsonb | تاریخچه کامل گفتگو |
| `status` | text | `new` / `contacted` / `qualified` / `lost` |
| `source` | text | `website` یا `telegram` |
| `created_at` | timestamptz | زمان ایجاد |

---

## ۷) امتیازدهی لید (Lead Scoring)

| فاکتور | امتیاز |
|---|---|
| بودجه مشخص اعلام کرده | +۳۰ |
| نیاز فوری / شروع نزدیک | +۲۰ |
| شماره تماس یا تلگرام داده | +۲۰ |
| تصمیم‌گیرنده است (مدیر/مالک/صاحب کسب‌وکار) | +۳۰ |
| **حداکثر** | **۱۰۰** |

**دسته‌بندی:**
- 🔵 سرد: کمتر از ۴۰ → پیگیری معمولی
- 🟡 گرم: ۴۰ تا ۷۰ → پیگیری همان روز
- 🔥 داغ: بالای ۷۰ → اعلان فوری به تیم + ایمیل

**آستانه اعلان به تیم:** امتیاز ≥ ۶۰

---

## ۸) اعلان به تیم

### قالب پیام تلگرام تیم
```
🔥 لید جدید فَر

👤 نام: علی رضایی
🏢 شرکت: کلینیک زیبایی رضا
🎯 خدمت: طراحی سایت + سئو
📊 امتیاز: 85% — داغ
📱 تماس: 0912xxxxxxx
🕐 زمان: 1405/05/02 - 14:32
```

### قالب ایمیل
موضوع: `لید جدید Far AI — [نام] — [خدمت]`
متن: نام، شرکت، خدمت، امتیاز، خلاصه مکالمه (کوتاه)، لینک مشاهده در داشبورد

---

## ۹) معماری فنی

```
           ┌───────────────┐   ┌───────────────┐
           │  وب‌سایت فَر   │   │  تلگرام فَر    │
           │ (پلاگین وردپرس│   │  (BotFather)  │
           │  — ویجت چت)   │   │               │
           └──────┬────────┘   └───────┬───────┘
                  │  HTTPS/JSON        │  Webhook/Polling
                  ▼                    ▼
           ┌──────────────────────────────────────┐
           │        Far AI API (FastAPI)          │
           │  POST /chat  +  تشخیص سرویس          │
           └───────┬──────────────┬───────────────┘
                   │              │
                   ▼              ▼
        ┌────────────────┐   ┌────────────────┐
        │  LLM (OpenAI)  │   │  PostgreSQL    │
        │  System Prompt │   │  (لیدها و گفتگو)│
        │  + دانش فَر    │   └────────────────┘
        └────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │  Notification: تلگرام تیم + ایمیل    │
        └──────────────────────────────────────┘
```

### استک پیشنهادی
| لایه | تکنولوژی |
|---|---|
| Backend | Python + FastAPI |
| AI | OpenAI API (برای MVP: مدل اقتصادی مثل `gpt-4o-mini`) |
| Bot | aiogram (Python) |
| Database | PostgreSQL |
| Deployment | Docker + Docker Compose + Nginx |
| Automation (اختیاری) | n8n (برای فازهای بعدی) |

---

## ۱۰) استقرار و زیرساخت

### مشخصات VM پیشنهادی (MVP)
- Ubuntu 24.04 LTS
- 2 vCPU / 4GB RAM / 50GB SSD
- IP استاتیک + دامنه (مثلاً `api.far.agency`)

### چرا Docker؟
تمام سرویس‌ها (API، Bot، PostgreSQL، Nginx) به صورت کانتینر تعریف می‌شوند تا **سیستم قابل انتقال بین هر میزبان** باشد:

```
VPS اروپایی (شروع)  →  Google Cloud VM  →  هر جای دیگر
        └───────────── فقط جابه‌جایی کانتینرها ─────────────┘
```

- برای MVP: VPS اروپایی (Hetzner / Contabo) — ارزان‌تر و بدون دردسر تحریم
- وقتی پروژه جدی شد: انتقال به Google Cloud (Compute Engine) — پایداری و مقیاس‌پذیری

---

## ۱۱) امنیت نسخه ۱

- کلیدها (OpenAI / Bot Token / DB) فقط در `.env` — هرگز در گیت
- Rate Limit روی `POST /chat` (مثلاً ۱۰ درخواست در دقیقه به ازای هر آیدی)
- CORS فقط برای دامنه فَر
- Backup روزانه دیتابیس (اسکریپت + کرون‌جاب)
- HTTPS اجباری (Let's Encrypt)
- لاگ کامل مکالمات برای بازبینی

---

## ۱۲) تعریف Done نسخه ۱ (معیار پذیرش)

✅ سند حاضر تأیید شد
✅ VPS + Docker + Nginx بالا آمد
✅ `POST /chat` پاسخ می‌دهد (بدون AI)
✅ LLM متصل است و با System Prompt فَر پاسخ می‌دهد
✅ تلگرام Bot به API وصل است
✅ ویجت چت سایت (پلاگین وردپرس) به API وصل است
✅ مکالمات در PostgreSQL ذخیره می‌شوند
✅ امتیازدهی و اعلان تلگرام/ایمیل کار می‌کند
✅ تست ۲۰ سناریوی مکالمه (مشتری عجول / قیمت‌محور / مردد / حرفه‌ای)

---

## ۱۳) خارج از محدوده نسخه ۱ (برای فازهای بعدی)

- حافظه بلندمدت و RAG (جستجو در دانش فَر) — هفته ۴
- داشبورد مدیریت لیدها
- CRM کامل
- Proposal Generator
- چند Agent (فروش / پشتیبانی / تولید محتوا)
- تحلیل خودکار سایت مشتری

---

## ۱۴) نقشه اجرا (خلاصه)

| هفته | تمرکز | خروجی |
|---|---|---|
| ۱ | زیرساخت + مغز | VPS، FastAPI، اتصال AI، شخصیت اولیه |
| ۲ | کانال‌ها | Telegram Bot + ویجت چت سایت + مسیر نیازسنجی |
| ۳ | تبدیل به Agent | دیتابیس، ذخیره مکالمه، Lead Scoring، اعلان‌ها |
| ۴ | حرفه‌ای‌سازی | Knowledge Base، RAG اولیه، داشبورد ساده، امنیت، Deploy، Launch 🎯 |
